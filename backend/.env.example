# ================================
# BACKEND ENVIRONMENT VARIABLES
# ================================

# Server Configuration
PORT=8080
CLIENT_ORIGIN=http://localhost:5173

# ================================
# DATABASE CONFIGURATION
# ================================

# Option 1: Full DSN (recommended for production)
# DB_DSN=user:password@tcp(host:port)/dbname?charset=utf8mb4&parseTime=True&loc=Local

# Option 2: Individual variables (for local development)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=article_db

# ================================
# RENDER DEPLOYMENT EXAMPLE
# ================================
# PORT=10000
# CLIENT_ORIGIN=https://your-app.vercel.app
# DB_DSN=user:password@tcp(your-mysql-host:3306)/your_db?charset=utf8mb4&parseTime=True&loc=Local
